import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Ignite360 - High-Performance Affiliate Partnerships',
  description: 'Drive measurable revenue through proven affiliate partnerships. We connect brands with top publishers, creators, and media buyers who deliver real customers.',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}
